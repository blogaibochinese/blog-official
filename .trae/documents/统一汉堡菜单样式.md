1. **分析差异**：对比 `index.html` 和 `esim-card.html` 中汉堡菜单的样式代码
2. **复制完整样式**：将 `index.html` 中完整的汉堡菜单样式（包括响应式设计和激活状态）复制到 `esim-card.html`
3. **替换现有样式**：替换或补充 `esim-card.html` 中现有的汉堡菜单样式
4. **确保功能一致**：确保 `esim-card.html` 中的汉堡菜单具有与 `index.html` 相同的响应式行为和动画效果
5. **测试效果**：检查修改后的页面在不同屏幕尺寸下的表现

具体修改点：

* 复制 `index.html` 中 `.nav-menu` 的响应式样式（移动端滑入效果）

* 复制汉堡菜单激活状态的动画样式

* 确保移动端导航菜单的样式一致

* 调整相关的媒体查询断点和样式值

