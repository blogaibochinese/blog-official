1. **修改导航栏菜单项样式**：为 `.nav-link` 添加按钮样式，包括背景色、圆角、padding和过渡效果
2. **为每个菜单项添加独立配色**：使用 `nth-child()` 选择器为9个菜单项分别设置不同的渐变背景色
3. **增强悬停效果**：添加悬停时的颜色变化、缩放和阴影效果
4. **优化响应式设计**：确保在移动端视图下按钮效果依然良好
5. **保持设计协调性**：使用渐变色彩方案，确保整体视觉效果统一

**具体实现**：

* 修改 `.nav-link` 样式，添加 `padding: 8px 16px`、`border-radius: 20px`、`background: linear-gradient()` 和 `transition: all 0.3s ease`

* 为每个菜单项使用不同的渐变配色，如蓝色、紫色、粉色、绿色、橙色等

* 添加悬停效果：\`transform: scale(1.05

